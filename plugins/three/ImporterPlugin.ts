export type ImporterComponents = {
    isLoading: boolean;
    /** entities will be created and destroyed with this component to track all loading that happens.  */
    loadingMessage: string;

    isSceneObject: boolean;

    
}